#include <bits/stdc++.h>
using namespace std;

int main (){
    typedef long long ll;
    ll t, a, sum=0;
    cin >> t;
    while (t--){
        cin >> a;
        sum += a;
    }
    cout << sum << endl;
     
}
